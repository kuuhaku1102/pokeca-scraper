name: Scrape Kagura TCG

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */3 * * *'  # 3時間ごと

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Python and Node.js dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          npm install playwright  # ✅ ← これが抜けてると npx が失敗する

      - name: Install Playwright browsers
        run: |
          npx playwright install --with-deps  # ✅ ← ブラウザ本体を入れる

      - name: Run scraper
        env:
          GSHEET_JSON: ${{ secrets.GSHEET_JSON }}
          SPREADSHEET_URL: ${{ secrets.SPREADSHEET_URL }}
        run: python kagura_tcg_scraper.py

      - name: Upload debug HTML
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: kagura_debug
          path: kagura_debug.html
