name: Apply Custom Patch

on:
  workflow_dispatch:

permissions:
  contents: write   # push用の最低限権限（PATで上書きされる）

jobs:
  apply-patch:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # --3way に必要（浅いcloneだとapply失敗）

      - name: Apply patch file
        run: |
          git apply --3way patches/update_scrapers.patch

      - name: Commit and push changes (via PAT)
        env:
          # 🔹 個人アクセストークンを使用
          # リポジトリの Settings > Secrets and variables > Actions で
          # PERSONAL_ACCESS_TOKEN として登録しておく
          GITHUB_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "Apply scraping patch automatically" || echo "No changes to commit"
          git push
