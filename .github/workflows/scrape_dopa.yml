name: Scrape Dopa with Playwright

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */3 * * *'  # 3時間ごと実行

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Python dependencies
        run: |
          pip install -r requirements.txt
          python -m playwright install

      - name: Run scraper
        env:
          GSHEET_JSON: ${{ secrets.GSHEET_JSON }}
        run: python scrape_dopa_to_sheets_playwright.py
